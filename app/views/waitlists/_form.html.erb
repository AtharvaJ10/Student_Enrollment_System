<%= form_with(model: waitlist) do |form| %>
  <% if waitlist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(waitlist.errors.count, "error") %> prohibited this waitlist from being saved:</h2>

      <ul>
        <% waitlist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.text_field :student_id, value: @userid, :readonly => true, type: :hidden %>
  </div>
  <div>
    <%= form.text_field :course_id, value: @course.id, :readonly => true, type: :hidden %>
  </div>
  <% if @waitlist1.nil? %>
    <div>
      <%= form.submit %>
    </div>
  <% else %>
    <div>
      <p class="text-danger"> You are already waitlisted for this course.</p>
    </div>
  <% end %>
<% end %>